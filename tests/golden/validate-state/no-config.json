{
  "argv": [
    "bash",
    "<ROOT>/scripts/validate-state.sh"
  ],
  "created_dirs": [],
  "deleted": [],
  "exit_code": 1,
  "files": {},
  "stderr": [],
  "stdout": [
    "WARNINGS (1):",
    "  no FINDINGS-REPORT-*.md in <PROJECT>/fuzz/state (run /cc-fuzzer:report to generate)",
    "",
    "ERRORS (2):",
    "  state exists but <PROJECT>/fuzz/state/fuzz-config.json is missing. v0.30 is multi-harness only — run 'harness-set.sh init --entry <fn>' (or /cc-fuzzer:campaign) to declare a harness set.",
    "  missing <PROJECT>/fuzz/state/schema-version. v0.30 requires schema v12; older campaigns cannot be migrated. Start a fresh campaign with /cc-fuzzer:campaign.",
    "",
    "FAIL: state validation failed. See errors above.",
    "  - Run '/fuzz-reset' to wipe state and start over (v0.30 requires schema v12; older state cannot be migrated)",
    "  - Or fix individual issues manually"
  ]
}
