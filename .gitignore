.gitignore
.git
.claude

flake.lock
rw.sh

# Python bytecode (cve_*.py / code_review_prescan.py imports)
__pycache__/
*.pyc

# Campaign workspace + build artifacts
fuzz/
# ...except the synthetic fixture campaigns the test suite runs against.
!/tests/fixtures/*/fuzz/
*.tar.gz
